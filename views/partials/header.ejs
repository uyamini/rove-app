<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Bootstrap CSS for styling the navbar -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <nav>
        <img src="/images/camera.svg" alt="Camera Icon" style="height: 30px;">
        <a href="/" <%- title === 'Home' ? 'class="active"' : '' %>>HOME</a>
        <a href="/activities" <%- title === 'Activities' ? 'class="active"' : '' %>>ACTIVITIES</a>
        <% if (user) { %>
            <a href="/activities/new" <%- title === 'Add Activity' ? 'class="active"' : '' %>>ADD ACTIVITY</a>
            <a href="/logout">LOG OUT</a>
        <% } else { %>
            <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png" alt="Google Icon"></a>
        <% } %>
    </nav>
</body>
</html>
